[request_definition]
r = sub, obj, act, org

[policy_definition]
p = sub, obj, act, org
g = _, _

[policy_effect]
e = some(where (p.eft == allow))

[matchers]
m = g(r.sub, p.sub) && r.obj == p.obj && r.act == p.act && r.org == p.org 